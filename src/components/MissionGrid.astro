<div class="mission-grid-wrapper">
  <slot />
</div>

<style>
  .mission-grid-wrapper {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 2rem;
    margin: 2rem 0 4rem;
  }

  .mission-grid-wrapper :global(.program-card) {
    text-decoration: none !important;
    background: var(--sl-color-gray-6);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 12px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  .mission-grid-wrapper :global(.program-card:hover) {
    transform: translateY(-4px);
    border-color: #d4af37;
  }

  .mission-grid-wrapper :global(.image-wrapper) {
    height: 200px;
    width: 100%;
    overflow: hidden;
  }

  .mission-grid-wrapper :global(.program-img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .mission-grid-wrapper :global(.program-title) {
    padding: 1.25rem;
    text-align: center;
    margin: 0;
    font-weight: 600;
    color: var(--sl-color-white);
  }
</style>